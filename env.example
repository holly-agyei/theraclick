## Copy this file to `.env.local` (Next.js reads `.env.local`).
## If you cannot create dotfiles in your environment, create `.env.local` manually and paste these keys.

## Firebase (Client) - required for real persistence/auth beyond demo mode
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

## AI (Server) - optional; app falls back to safe demo replies if missing
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1

## Gemini (Server) - preferred if set
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

## Admin approvals (Server)
ADMIN_API_KEY=
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=

## WebRTC TURN Servers (Optional - for better connectivity behind NATs/firewalls)
## Leave empty to use free STUN servers only (works for most cases)
## For production, consider using a TURN service like Twilio, or self-hosted coturn
NEXT_PUBLIC_TURN_SERVER_URL=
NEXT_PUBLIC_TURN_USERNAME=
NEXT_PUBLIC_TURN_CREDENTIAL=